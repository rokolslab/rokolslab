# Profile TODO

- TODO: Manually preview `README.preview.md` on GitHub after pushing to confirm mobile rendering and external widget loading.
- TODO: Decide whether to keep the typing SVG after visual review; the static header already carries the main identity.
- TODO: Run the `Generate contribution stream` workflow once after push so the `output` branch SVG assets are created.
- TODO: Add Telegram, LinkedIn, or website badge only if a public profile URL is confirmed.
- TODO: Check whether the four featured repositories are pinned in the same order as the featured pipelines section.
- TODO: Consider updating the GitHub profile repository description; `gh repo view` returned an empty description.
- TODO: `github-readme-stats` returned `503 DEPLOYMENT_PAUSED` during validation, so stats were intentionally omitted from the preview.
